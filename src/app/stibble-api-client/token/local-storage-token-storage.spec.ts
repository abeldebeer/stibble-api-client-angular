import {LocalStorageTokenStorage} from './local-storage-token-storage';

describe('LocalStorageTokenStorage', () => {
  it('should create an instance', () => {
    expect(new LocalStorageTokenStorage()).toBeTruthy();
  });
});

import {AbstractTokenStorage} from './abstract-token-storage';

describe('AbstractTokenStorage', () => {
  it('should create an instance', () => {
    expect(new AbstractTokenStorage()).toBeTruthy();
  });
});
